{{#isDubboClient}}
# Dubbo Config properties
## ApplicationConfig Bean
spring.application.name= dubbo-demo-client
## RegistryConfig Bean

{{#dep_zookeeper}}
spring.dubbo.registry = zookeeper://localhost:2181?client=curator
{{/dep_zookeeper}}

{{/isDubboClient}}
{{#isDubboServer}}
spring.application.name = dubbo-demo-server
spring.dubbo.server=true

## RegistryConfig Bean
{{#dep_zookeeper}}
spring.dubbo.registry = zookeeper://localhost:2181?client=curator
{{/dep_zookeeper}}
{{/isDubboServer}}